{
  "description": "Claude Sketch hooks: auto-approve sketch tools",
  "hooks": {
    "PermissionRequest": [
      {
        "type": "command",
        "command": "${CLAUDE_PLUGIN_ROOT}/hooks/auto-approve-sketch.sh"
      }
    ]
  }
}
